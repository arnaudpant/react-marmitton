import React from "react";
import ContainerCards from "../components/container-cards/ContainerCards";

const Home = () => {
    return (
        <>
            <ContainerCards />
        </>
    );
};

export default Home;
